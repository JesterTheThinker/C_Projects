#include<stdio.h>

int arpr(int);

int main()
{
    int a[5];
    int i;
    printf("enter values of array: ");
    for (i = 0; i < 5; i++)
    {
        scanf("%d", a[i]);
    }

    arpr(a);
    
}

int arpr(int a)
{
    
    printf("%d", a[])
}